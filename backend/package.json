{
  "name": "taskflow-backend",
  "version": "1.0.0",
  "description": "TaskFlow API — CRUD de tarefas com Node.js + Express + SQLite",
  "main": "src/server.js",
  "scripts": {
    "start": "node --experimental-sqlite src/server.js",
    "dev": "node --experimental-sqlite --watch src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.3",
    "helmet": "^7.1.0",
    "uuid": "^9.0.1"
  }
}
